Author: José Manuel Santamaría Lema <panfaust@gmail.com>
Description: Fix build failure, link to the KDE libraries needed.
Forwarded: not-needed
Applied-Upstream: 4.6.2, commit:1223184

--- a/kalgebra/mobile/CMakeLists.txt
+++ b/kalgebra/mobile/CMakeLists.txt
@@ -5,6 +5,8 @@ kde4_add_executable(kalgebramobile
 
 target_link_libraries(kalgebramobile ${KDE4_KDEUI_LIBS} ${QT_QTSCRIPT_LIBRARY}
 # 				${QT_QTSCRIPTTOOLS_LIBRARY}
+				${KDE4_KDECORE_LIBS}
+				${KDE4_KDEUI_LIBS}
 				analitza analitzagui)
 
 install(TARGETS kalgebramobile ${INSTALL_TARGETS_DEFAULT_ARGS})
